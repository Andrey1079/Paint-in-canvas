class DrawFigure {
  constructor(canvas, tempCanvas, settingsObj, buttonSelector) {
    this.button = documet.querySelector(buttonSelector);
    this.basicCanvas = documet.querySelector(settingsObj.basicCanvasSelector);
  }

  drawFigure() {}
}
